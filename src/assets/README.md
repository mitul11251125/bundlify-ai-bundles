# Source Assets

This folder is created for storing source assets, as requested. In the Shopify React Router template, files are primarily organized inside the `/app` folder. You can use this `/src/assets` folder or `/app/assets` depending on your preference.
