# App Assets

Store your app-specific assets here (e.g. logos, icons, styling assets, internal images) that will be bundled by Vite.

You can import them in React Router components like this:
```tsx
import logo from "./logo.png";
```
